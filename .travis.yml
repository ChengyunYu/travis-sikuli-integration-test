language: go

go:
  - "1.10"

services:
  - xvfb

before_install:
  - sudo apt-get update

install:
  - sudo apt-get update
  - sudo apt install libopencv-dev
  - sudo apt install tesseract-ocr

before_script:
  - xvfb-run java -jar sikulixsetup-1.1.3.jar options 1.1

script:
  - ls
  - sudo ~/sikulix/runsikulix -r ~/Desktop/SikulixRegressionScripts/MultiTenantAPP.sikuli
